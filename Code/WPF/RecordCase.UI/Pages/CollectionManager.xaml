<UserControl x:Class="RecordCase.UI.Pages.CollectionManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:command="http://www.galasoft.ch/mvvmlight"
             xmlns:collections="clr-namespace:RecordCase.UI.Controls.Collections"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Collections, Source={StaticResource Locator}}"
             >
    <ScrollViewer>
        <StackPanel MinWidth="200" Orientation="Vertical">
            <mui:BBCodeBlock BBCode="[url=/Pages/CollectionAdd.xaml]Add new...[/url]" />
            
            <!--Collection List-->
            <collections:CollectionList ItemsSource="{Binding Collections}"
                     Width="200"
                     Height="100"
                     Background="White"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
